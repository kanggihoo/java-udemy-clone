해결 방법 및 수정 사항
위 문제들을 해결하기 위해 다음과 같은 작업을 수행했습니다.

① Repository 이름 수정 및 고정
CategoryRepositoriy의 오타를 CategoryRepository 로 수정하였습니다.

② Service 구현체 (CategoryServiceImpl) 전면 재작성

- CategoryRepository 를 생성자 주입 방식으로 연결했습니다.
- 모든 메서드에서 메모리 리스트(/categories) 대신 categoryRepository를 통해 DB에 접근하도록 수정했습니다.
- Category(DTO)와 CategoryEntity 간의 변환 로직을 추가했습니다.

③ H2 DB 및 Spring Data JPA 설정 (application.properties)

- H2 인 메모리 DB를 사용하고 웹 콘솔을 활성화하는 설정을 추가했습니다.

④ 의존성 추가 (pom.xml)

- H2 DB 구동을 위해 필요한 라이브러리를 추가하고 Spring Boot 스타터를 정리했습니다.
