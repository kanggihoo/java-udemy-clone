1. postgres Sql로 연결 후 script 이용해서 데이터 생성 => 그냥 간단하게 application.properties db 설정으로 해결 => OK
2. custom 방식으로 DTO , Entity 변환 => modelMapper 사용하지 않고
   => d완료
3. 전역 핸들러 exception 구현
4. JAP 관련 페이지 네이션 구현 및 CRUD 구현 => 완료
5. 서비스 계층에서 DTO 연결해서 Entity -> DTO -> API Response 변환 => 완료
6.

## 개선사항

1. dto/request/CategoryPageRequest.java => 여기서 정렬조건을 ENUM으로 해도 되지 않나?
2. 유효성 검증 로직을 어디에 반영하는게 좋을지??
